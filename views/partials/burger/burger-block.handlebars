<li class="list-group-item">
    {{burger_name}}
    <button class="change-devour btn btn-warning" data-id="{{id}}" data-newdevour="{{devour}}">
        {{#if devoured}}NO THANK YOU!{{else}}NOM NOM{{/if}}
    </button>
</li>
